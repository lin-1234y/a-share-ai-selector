# A股智能选股产品原则

## 第一原则：小白可用

这个工具面向不懂编程、不懂数据库、不想记命令的使用者。所有核心功能都应该尽量做到：

- 双击启动，不要求用户打开命令行。
- 电脑和手机都能用网页访问。
- 页面上有清楚按钮，不让用户猜下一步。
- 数据更新、个股查询、选股、导出都应该一键完成。
- 能用 Excel/WPS 打开的 CSV，就不要强迫用户打开数据库。
- 出错时用普通话说明原因和下一步，不显示一堆技术报错。
- 复杂功能可以藏在后面，但常用流程必须简单。

## 功能设计要求

- 默认给出推荐设置，用户不需要理解参数也能用。
- 每个慢操作都要显示进度、状态和是否完成。
- 能自动恢复的事情自动恢复，不能恢复时告诉用户该点哪个按钮。
- 数据库、API Key、GitHub、Python 环境都不应该暴露成日常操作。
- 每次新增功能，都要问一句：用户是否可以不懂编程也完成这件事？

## 当前默认使用方式

- 启动系统：双击 `启动A股智能选股.bat`。
- 查看网页：打开 `http://127.0.0.1:8766/`。
- 导出个股行情：双击 `导出个股行情.bat`，输入股票代码。
- 查看导出文件：打开 `exports` 文件夹里的 CSV。

本项目所有后续版本都应优先维护这个原则。
